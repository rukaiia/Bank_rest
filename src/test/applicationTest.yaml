spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bank_test
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

security:
  disabled: true
